
import Khadang 


class surush:
	proxy=[]
	
	def loadWS(self,arg):
		pass
	def  ezLoad(self,arg):
		pass
	def	verify(self,arg):
		#verify the page actually has loaded fully with no errors
		#Returns the content and a flag which explains the extent of issues witht the loaded page.
		
	def private setProxy(self,arg):
	self.proxy=arg

	def private bezar(self,arg):
	#write to disk?



	def private begir(self,cursor,arg):
		conent=[]
		ros=khandang.getRosetaByURL(cursor,arg)
		if ros.proxy:
			#setProxy(ros.Proxy):  call a function? or  set a local variable?
		if ros.virtual:
		 #call selenium load
		else:
		#load normal
		return conent
	def begirBezar(cursor,arg):
		bezar(begir(cursor,arg))
	def private bezar(self,conent):
	#write to file
	def  begirBebar(self,cursor,arg):
	return begir(cursor,arg)
	
	
	
	